import { CodeHighlightAutoredrawDirective } from './code-highlight-autoredraw.directive';

describe('CodeHighlightAutoredrawDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeHighlightAutoredrawDirective();
    expect(directive).toBeTruthy();
  });
});
